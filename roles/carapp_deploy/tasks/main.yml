# MySQL related things
---
  - block:
    - name: "Coping car file to ei-master"
      copy:
        src: "files/demo-products_1.0.0.car"
        dest: "/usr/lib/wso2/wso2ei/6.5.0/repository/deployment/server/"
        owner: "wso2carbon"
        group: "wso2"
        mode: 0755

    become: true
    become_method: sudo
    become_user: root